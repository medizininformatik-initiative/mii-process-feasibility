stores:
  ${STORE_ID}:
    baseUrl: ${BASE_URL}
    evaluationStrategy: ccdl
    requestTimeout: ${TIMEOUT}

networks:
  medizininformatik-initiative.de:
    obfuscate: true
    stores:
    - ${STORE_ID}
